/**
 * Check if all desktop packages are installed for the app to run correctly
 * @returns Promise for if the check passed
 */
export default function packagesInstalled(): Promise<void> {
    return new Promise((resolve, reject) => {

    });
}   